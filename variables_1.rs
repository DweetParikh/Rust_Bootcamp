fn main(){
    let x: i32 = -5; //by defaults i32 gets assigned 
    let y: u32 = 1000; //unsigned integer, cannot have negative numbers
    let z: f64 = 1000.001; //float for adding decimals number

    println!("x: {}, y: {}, z: {}", x, y, z);
}
